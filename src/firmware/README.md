# src/firmware

Future original SHINO-80 firmware source lives here.

Candidate content:

- BIOS
- monitor
- boot code
- ROM source/build inputs

Do not place proprietary ROM dumps or existing machine BIOS images here.

Firmware is software executed by the emulated CPU; it must remain conceptually separate from JavaScript emulator/device code.
