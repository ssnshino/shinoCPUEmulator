# working-logs

作業記録を管理する。

- Current: `working-logs/head/`
- Historical: `working-logs/history/`

PLANに対して何を実施し、何を見送り、何が判明したかを残す。

`head/`は現在のrelease/active workだけに絞る。完了済みフェーズの記録は
内容を書き換えずhistoryへ移す。
